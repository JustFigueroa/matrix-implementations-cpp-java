// Source code is decompiled from a .class file using FernFlower decompiler (from Intellij IDEA).
import java.io.File;
import java.io.FileNotFoundException;
import java.util.Scanner;

public class Matrix {
   private float[][] data;
   private int rows;
   private int cols;

   public Matrix(int var1, int var2) {
      if (var1 >= 1 && var2 >= 1) {
         if (var1 <= 100 && var2 <= 100) {
            this.rows = var1;
            this.cols = var2;
            this.data = new float[var1][var2];
         } else {
            throw new IllegalArgumentException("Matrix dimensions should be between 1 and 100");
         }
      } else {
         throw new IllegalArgumentException("Matrix dimensions should be between 1 and 100");
      }
   }

   public void setValue(int var1, int var2, float var3) {
      this.data[var1][var2] = var3;
   }

   public Matrix add(Matrix var1) {
      if (this.rows == var1.rows && this.cols == var1.cols) {
         Matrix var2 = new Matrix(this.rows, this.cols);

         for(int var3 = 0; var3 < var1.rows; ++var3) {
            for(int var4 = 0; var4 < var1.cols; ++var4) {
               var2.data[var3][var4] = this.data[var3][var4] + var1.data[var3][var4];
            }
         }

         return var2;
      } else {
         throw new IllegalArgumentException("Incompatable array sizes");
      }
   }

   public Matrix sub(Matrix var1) {
      if (this.rows == var1.rows && this.cols == var1.cols) {
         Matrix var2 = new Matrix(this.rows, this.cols);

         for(int var3 = 0; var3 < var1.rows; ++var3) {
            for(int var4 = 0; var4 < var1.cols; ++var4) {
               var2.data[var3][var4] = this.data[var3][var4] - var1.data[var3][var4];
            }
         }

         return var2;
      } else {
         throw new IllegalArgumentException("Incompatable array sizes");
      }
   }

   public Matrix mult(Matrix var1) {
      if (this.cols != var1.rows) {
         throw new IllegalArgumentException("Incompatable array sizes");
      } else {
         Matrix var2 = new Matrix(this.rows, this.cols);

         for(int var3 = 0; var3 < this.rows; ++var3) {
            for(int var4 = 0; var4 < var1.cols; ++var4) {
               var2.data[var3][var4] = 0.0F;

               for(int var5 = 0; var5 < this.cols; ++var5) {
                  float[] var10000 = var2.data[var3];
                  var10000[var4] += this.data[var3][var5] * var1.data[var5][var4];
               }
            }
         }

         return var2;
      }
   }

   public void displayMatrices() {
      for(int var1 = 0; var1 < this.rows; ++var1) {
         for(int var2 = 0; var2 < this.cols; ++var2) {
            System.out.print(this.data[var1][var2] + " ");
         }

         System.out.println();
      }

   }

   private static Matrix manualMatrixGenerator(Scanner var0, String var1) {
      System.out.print(var1 + " rows: ");
      int var2 = var0.nextInt();
      System.out.print(var1 + " columns: ");
      int var3 = var0.nextInt();
      Matrix var4 = new Matrix(var2, var3);

      for(int var5 = 0; var5 < var2; ++var5) {
         for(int var6 = 0; var6 < var3; ++var6) {
            System.out.print(var1 + "[" + var5 + "] [" + var6 + "]: ");
            var4.setValue(var5, var6, var0.nextFloat());
         }
      }

      return var4;
   }

   private static Matrix[] fileMatrixInitialization(Scanner var0) {
      System.out.print("Enter path to file: ");
      var0.nextLine();
      String var1 = var0.nextLine();

      try {
         Scanner var2 = new Scanner(new File(var1));

         Matrix[] var5;
         try {
            Matrix var3 = fileMatrixGenerator(var2);
            Matrix var4 = fileMatrixGenerator(var2);
            var5 = new Matrix[]{var3, var4};
         } catch (Throwable var7) {
            try {
               var2.close();
            } catch (Throwable var6) {
               var7.addSuppressed(var6);
            }

            throw var7;
         }

         var2.close();
         return var5;
      } catch (FileNotFoundException var8) {
         throw new IllegalArgumentException("File could not be opened: " + var1);
      }
   }

   private static Matrix fileMatrixGenerator(Scanner var0) {
      int var1 = var0.nextInt();
      int var2 = var0.nextInt();
      Matrix var3 = new Matrix(var1, var2);

      for(int var4 = 0; var4 < var1; ++var4) {
         for(int var5 = 0; var5 < var2; ++var5) {
            var3.data[var4][var5] = var0.nextFloat();
         }
      }

      return var3;
   }

   private static int chooseOperation(Scanner var0) {
      System.out.println("0. Select new matrices");
      System.out.println("1. Addition");
      System.out.println("2. Subtraction");
      System.out.println("3. Multiplication");
      System.out.print("Selection: ");
      return var0.nextInt();
   }

   public static void main(String[] var0) {
      Scanner var1 = new Scanner(System.in);
      boolean var2 = true;
      boolean var3 = false;
      boolean var4 = false;

      while(var2) {
         Matrix var5 = null;
         Matrix var6 = null;

         for(Object var7 = null; !var3; var3 = true) {
            System.out.println("---------------------");
            System.out.println("Choose an entry method");
            System.out.println("---------------------");
            System.out.println("0.Quit program");
            System.out.println("1.Manual Entry");
            System.out.println("2.Read from file");
            System.out.println("Selection: ");
            int var8 = var1.nextInt();

            try {
               switch (var8) {
                  case 0:
                     var2 = false;
                     break;
                  case 1:
                     var5 = manualMatrixGenerator(var1, "Matrix A");
                     var6 = manualMatrixGenerator(var1, "Matrix B");
                     var3 = true;
                     break;
                  case 2:
                     var1.nextLine();
                     Matrix[] var9 = fileMatrixInitialization(var1);
                     var5 = var9[0];
                     var6 = var9[1];
                     var3 = true;
                     break;
                  default:
                     System.out.println("Invalid selection. Enter 1 or 2");
               }
            } catch (IllegalArgumentException var10) {
            }
         }

         System.out.println("Matrix A: ");
         var5.displayMatrices();
         System.out.println("Matrix B: ");
         var6.displayMatrices();

         while(var2 && !var4) {
            int var17 = chooseOperation(var1);

            try {
               switch (var17) {
                  case 0:
                     var4 = true;
                     break;
                  case 1:
                     Matrix var16 = var5.add(var6);
                     System.out.println("Matrix C: ");
                     var16.displayMatrices();
                     break;
                  case 2:
                     Matrix var15 = var5.sub(var6);
                     System.out.println("Matrix C: ");
                     var15.displayMatrices();
                     break;
                  case 3:
                     Matrix var14 = var5.mult(var6);
                     System.out.println("Matrix C: ");
                     var14.displayMatrices();
                     break;
                  default:
                     System.out.println("Invalid selection. Enter 0 thorugh 3");
               }
            } catch (IllegalArgumentException var11) {
            }
         }
      }

      var1.close();
      System.out.println("Program Exiting");
   }
}
